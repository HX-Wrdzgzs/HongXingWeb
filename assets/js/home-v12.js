(()=>{const d=document;const header=d.querySelector('.hx-header');const reveals=[...d.querySelectorAll('.hx-reveal')];const menuBtn=d.querySelector('.hx-menu');const mobile=d.querySelector('.hx-mobile-menu');const stage=d.querySelector('.hx-stage');const onScroll=()=>{if(!header)return;header.classList.toggle('is-scrolled',scrollY>8)};onScroll();addEventListener('scroll',onScroll,{passive:true});if(!matchMedia('(prefers-reduced-motion: reduce)').matches&&'IntersectionObserver'in window){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.1,rootMargin:'0px 0px -8%'});reveals.forEach(el=>io.observe(el))}else{reveals.forEach(el=>el.classList.add('in'))}if(menuBtn&&mobile){menuBtn.addEventListener('click',()=>{const open=mobile.classList.toggle('open');menuBtn.setAttribute('aria-expanded',open?'true':'false')})}if(stage&&!matchMedia('(prefers-reduced-motion: reduce)').matches&&matchMedia('(min-width: 761px)').matches){const base='rotateX(-5deg) rotateY(-11deg)';stage.style.transform=base;stage.parentElement?.addEventListener('pointermove',e=>{const r=stage.parentElement.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;stage.style.transform=`rotateX(${-5-y*10}deg) rotateY(${-11+x*16}deg) rotateZ(${x*2}deg)`});stage.parentElement?.addEventListener('pointerleave',()=>stage.style.transform=base)}})();